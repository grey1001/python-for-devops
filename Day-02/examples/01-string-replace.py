text = "Python is awesome"
new_text = text.replace("awesome", "great") # used to replace a string with another string. The first argument is the string to be replaced and the second argument is the string to replace it with.
print("Modified text:", new_text)
